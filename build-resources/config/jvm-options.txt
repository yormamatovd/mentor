# JVM Options for Mentor Application
# These options are used when launching the application

# Memory settings (adjust based on your application's needs)
-Xms256m
-Xmx1024m

# Garbage collection options (G1GC is recommended for desktop applications)
-XX:+UseG1GC
-XX:MaxGCPauseMillis=200

# Performance optimizations
-XX:+UseStringDeduplication
-XX:+OptimizeStringConcat

# JavaFX specific options
-Dprism.order=sw
-Dprism.verbose=false
-Djavafx.animation.pulse=60

# Application specific
-Dfile.encoding=UTF-8
-Dsun.java2d.opengl=true

# Logging (uncomment for debugging)
# -Dlogback.configurationFile=config/logback.xml
# -Dorg.slf4j.simpleLogger.defaultLogLevel=DEBUG
